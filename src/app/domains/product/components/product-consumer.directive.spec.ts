import { ProductConsumerDirective } from './product-consumer.directive';

describe('ProductConsumerDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductConsumerDirective();
    expect(directive).toBeTruthy();
  });
});
